
package com.example.stresscure;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class StressCureApplication {
    public static void main(String[] args) {
        SpringApplication.run(StressCureApplication.class, args);
    }
}

// This is the main entry point of the Spring Boot application.
// It launches the embedded server and initializes all Spring components.